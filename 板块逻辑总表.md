# 财务工作台 · 板块逻辑总表

> **文档目的**：把工作台全部功能模块的业务逻辑系统化整理成表格，便于查阅、培训与后续扩展。  
> **数据来源**：基于 `财务工作台/js` 下各模块真实源码梳理（2026-08-02 版）。  
> **阅读提示**：本工作台存在**两套平行账套**——「登记内账」（单式流水）与「报税记账」（复式凭证），二者数据各自独立，详见第五节。



---

## 一、模块总览表

| #  | 导航标识         | 模块名称      | 一句话定位                    | 数据归属    |
| -- | ------------ | --------- | ------------------------ | ------- |
| 1  | home         | 工作台首页     | 经营概览 / 老板看账，聚合各模块指标      | 只读聚合    |
| 2  | internal     | 登记内账      | 日常收支流水与账户余额（单式账）         | 内账体系    |
| 3  | tax          | 报税记账      | 凭证 / 账簿 / 固定资产 / 财报（复式账） | 外账体系    |
| 4  | contacts     | 往来账       | 应收 / 应付 / 预付与账龄、核销       | 内账体系    |
| 5  | memo         | 备忘录       | 待办提醒、到期管理                | 全局共享    |
| 6  | taxcalc / taxburden / stamptax / disabilityfund / iitwithhold | 税负测算 / 税负率计算 / 印花税计算 / 残保金测算 / 个税累计预扣表 | 税费测算工具集（小规模税负预警、四口径税负率、印花税、残保金、个税累计预扣） | 独立（不落库） |
| 7  | taxkb / fnkb | 财税 / 公式知识 | 内置财税与公式参考库               | 内置常量    |
| 8  | reports      | 报表中心      | 基于内账的老板视角报表              | 内账体系    |
| 9  | invoices     | 发票台账      | 进销项发票 / 合同 / 库存与增值税测算    | 独立测算    |
| 10 | salary       | 工资登记      | 员工月度工资登记与累计汇总            | 内账体系    |
| 11 | projectCost  | 项目核算      | 按项目核算利润 / 利润率 / 单产       | 内账下游    |
| 12 | version      | 版本与备份     | 版本日志、备份恢复（含照片）           | 全局      |

---

## 二、数据存储架构

本工作台数据层约定（`db.js`）：文本数据走 `localStorage`（前缀 `fw_`），照片凭证走 `IndexedDB`。

- **全局键（跨账本共享，不被命名空间隔离）**：  
  `sites, memos, kb_notes, ledgers, currentLedger, tax_templates, fw_rollback_request, fw_backup_log`
- **按账本隔离键（自动追加 `_<账本ID>`）**：  
  `internal, internal_budget, internal_cats, internal_openings, internal_accounts,
  tax_vouchers, tax_journals, tax_statements, tax_openings, tax_assets,
  contacts, invoices, contracts, stock, salary_employees, salary_records, project_qty`  
  （另含 `tax_filings, tax_calendar_custom` 预留键）

---

## 三、分模块逻辑详表

### 1. 工作台首页 home（dashboard.js）

- **定位**：老板视角经营概览，只读聚合各模块指标。
- **功能要点**：本月收入/支出/结余卡片与环比；本月预算使用率；近 6 个月收支趋势折线图；各账户余额看板（复用 `FW.internalCalc.accountBalancesTree`）；各项目盈亏看板（复用 `FW.projectCostCalc.compute('all')`）；待办提醒（来自 `memos`，区分逾期/未来 7 天）；最近流水（取 `internal` 前 6 条）。
- **关键规则**：收入−支出按月汇总；`refund` 冲减支出不计入总收入；预算率 = 本月支出 / 当月预算总额（无预算显示"未设置"）。
- **对外联动**：只读复用 `internalCalc` 与 `projectCostCalc`，点击看板跳转 `internal` / `projectCost`。

### 2. 登记内账 internal（internal.js）

- **定位**：日常收支流水 / 凭证（单式流水账），资金层各账户余额管理。
- **功能要点**：流水登记（收入/支出/退款/互转/股本等多类型）；账户余额（含期初）；期初余额设置；预算设置与对比；自定义账户（一/二级层级）；银行对账；流水筛选（支持二级分类联动）。
- **存储键**：`internal`(流水)、`internal_openings`(期初)、`internal_budget`(预算)、`internal_cats`(分类)、`internal_accounts`(账户树)。
- **关键规则**：
  - 账户余额 = 期初 + 本期收支(收入−支出) + 本期往来(互转净额 + 股本净)。
  - 资金总计 = Σ账户余额 = 期初总计 + 区间结余 + 股本净变动。
  - 退款 `refund` 不计入总收入，冲减支出（与 dashboard、project_cost 口径一致）。
  - 区间经营结余 `netProfit = Σ收入 − Σ支出（+ 退款）`。
- **对外联动**：导出 `FW.internalCalc`（balances/breakdown/openingsTotal/netProfit/equityNet/filterRows/cat2Name 等）；被 reports、dashboard、project_cost 大量读取；被 contacts 核销时写入。

### 3. 报税记账 tax（tax.js）

- **定位**：柠檬云风格凭证 / 账簿 / 固定资产 / 报表（复式记账，由记账凭证自动汇总生成账表）。
- **功能要点**：记账凭证（借贷平衡校验）；常用凭证模板（计提工资/折旧/附加税/所得税、结转收入成本、交税等）；会计科目表（内置 `CHART` + 自定义）；明细账 / 科目余额表；固定资产与折旧；月末结转损益；三大财务报表（资产负债表/利润表/现金流量表）；期初余额编辑。
- **存储键**：`tax_vouchers`(凭证)、`tax_journals`(日记账)、`tax_statements`(报表)、`tax_openings`(期初)、`tax_assets`(固定资产)；全局键 `tax_templates`(模板)。
- **关键规则**：
  - 固定资产折旧（直线法按月）：`月折旧 = (原值 − 残值) / (年限×12)`，残值 = 原值×残值率（默认残值率 5%、年限 5 年）。
  - 月末结转损益：收入类科目→贷记本年利润；费用类科目→借记本年利润；净利润转入本年利润。
  - 报表取数：资产负债表经 `normBal(科目)` 取正常方向余额；利润表由损益科目自动计算（注明所得税未自动计提）。
- **对外联动**：与 `internal` 是**两套独立账套**，报表互不共用数据。

### 4. 往来账 contacts（contacts.js）

- **定位**：应收 / 应付 / 预付管理（客户/供应商），含账龄分析与核销联动。
- **功能要点**：往来单登记（应收/应付/预付，可关联项目）；余额统计与净头寸；账龄分析（0–30/31–60/61–90/90+ 天）；核销（应收收款/应付付款/预付核销，可一键登记到内账）。
- **存储键**：`contacts`（按账本隔离）。
- **关键规则**：
  - 余额 `balance = amount − settled`；账龄按余额>0 时距发生日天数。
  - 核销联动：应收核销→在 `internal` 写收入；应付核销→写支出；预付核销**不**登记内账（仅更新 settled）。
- **对外联动**：写 `internal`（应收/应付核销）；被 `project_cost` 读取（预付且 balance>0 带关联项目→进入该项目"应收回款项"）。

### 5. 备忘录 memo（memo.js）

- **定位**：待办与提醒（分类、置顶、搜索、到期）。
- **功能要点**：备忘列表、分类筛选、关键词搜索、置顶、到期日标记、增删改。
- **存储键**：`memos`（全局键，跨账本共享）。
- **关键规则**：排序优先置顶 `pin`，再按 `updatedAt` 倒序；过期判定 `due < FW.today()`。
- **对外联动**：首页 `home` 读取生成待办提醒；自身不写其它模块。

### 6. 税负测算 taxcalc（taxcalc.js）

- **定位**：小规模纳税人税负测算 / 预警（季报口径）。
- **功能要点**：输入季度含税销售额、征收率、城建税地区、主体类型、季度利润 → 实时测算增值税/附加税/所得税/综合税负率，并给免征临界、税负偏高等预警。
- **存储键**：无（纯计算工具，不落库）。
- **关键规则**：
  - 不含税销售额 = 含税销售额 ÷ (1+征收率)；季度不含税 ≤ 30 万免征增值税。
  - 增值税 `vat = 免征? 0 : 不含税×征收率`。
  - 附加税 = `vat × (城建[市区7%/县城5%/其他1%] + 教育3% + 地方教育2%) × 0.5`（六税两费减半）。
  - 所得税：有限公司小型微利年利润≤300万→5%，超出按25%简化；个体户五级超额累进（3万/9万/30万/50万/∞ 分档，2023–2027 利润≤200万再减半）。季度利润×4 估算年额再÷4 还原。
- **对外联动**：独立，不读写其它模块。

### 6.1 税负率计算 taxburden（taxburden.js）

- **定位**：四口径税负率计算 + 行业经验区间对比，用于「税负率异常」自查（税局风险扫描常用指标）。
- **功能要点**：输入不含税营业收入、增值税（直接填 / 销项−进项推导）、附加税费（自动 / 手工）、所得税、代扣个税、其他税费、利润总额、所属行业 → 实时输出增值税 / 流转税 / 所得税 / 综合四个税负率 + 实际所得税率 + 利润率，并给出偏低偏高的风险提示与改善建议。支持「📥 从内账带入本年数据」（调 `FW.reportsCalc.agg`）。
- **存储键**：无（纯计算工具，不落库）。
- **关键规则**：
  - 增值税税负率 = 应纳增值税 ÷ 不含税营业收入；流转税税负率 =（增值税+附加）÷ 不含税收入；所得税税负率 = 所得税 ÷ 不含税收入；综合税负率 = 税费合计 ÷ 不含税收入。
  - 实际所得税率 = 所得税 ÷ 利润总额（小微约 5%，法定 25%，>25% 提示纳税调增）。
  - 代扣代缴个税默认**不计入**企业税负，可勾选计入。
  - 分母为 0 时税负率取 0（除零保护）；增值税 = max(0, 销项−进项)（留抵不为负）。
  - 行业参考区间 `INDUSTRY`（12 项）：批发 0.9–1.5%、零售 1.5–2.5%、制造 2–3.5%、农副加工 1–2.5%、建筑 1.5–3%、运输 2–3%、餐饮 2–4%、现代服务 3–6%、软件 3–6%、租赁 3–5%、房地产 4–5%。
- **对外联动**：只读调用 `FW.reportsCalc.agg(from,to)` 带入内账年度收入/税金/利润；纯函数暴露在 `FW.taxBurdenCalc`（`calcBurden` / `judge` / `buildWarnings`），由 `test/tax_burden_test.js` 覆盖。

### 6.2 印花税计算 stamptax（stamptax.js）

- **定位**：按《印花税法》税目与比例税率逐项计算印花税并累加，供签合同/做账时快速估算。
- **功能要点**：动态多行明细（税目下拉 + 计税金额），实时累加应纳税额；支持"六税两费"减半（小规模/小微/个体户，证券交易印花税除外）；「＋ 添加一行」增删行。
- **存储键**：无（纯计算工具，不落库）。
- **关键规则**：应纳税额 = 计税金额 × 适用税率。内置 `STAMP` 税目表：借款 0.005%、买卖/建设工程/技术 0.03%、承揽/运输/产权转移(股权·不动产) 0.05%、租赁/仓储/保管/财产保险/证券交易 0.1%、营业账簿 0.025%。减半时除 `securities` 外所有行税率 ×0.5。
- **对外联动**：纯函数 `FW.stampTaxCalc.calcStamp(rows, half)` 由 `test/stamptax_test.js` 覆盖。

### 6.3 残保金测算 disabilityfund（disabilityfund.js）

- **定位**：测算残疾人就业保障金应缴额，判断免征/分档减缴情形。
- **功能要点**：输入上年在职职工人数、安排残疾人数、年平均工资、社平工资 2 倍上限（可选）→ 实时算应缴保障金，并标注安排比例、分档系数、是否免征。
- **存储键**：无（纯计算工具，不落库）。
- **关键规则**：`gap = N×1.5% − D`；`N≤30` 或 `gap≤0` → 免征；`gap>0` 时 `base = gap × cappedW`，分档系数 `ratio≥1% → 0.5`、`ratio<1% → 0.9`；`cappedW` 在启用封顶且 `W>上限` 时取上限。
- **对外联动**：纯函数 `FW.disabilityFundCalc.calcFund(N,D,W,capOn,cap)` 由 `test/disabilityfund_test.js` 覆盖。

### 6.4 个税累计预扣表 iitwithhold（iitwithhold.js）

- **定位**：工资薪金所得按累计预扣法（假设各月相同）生成全年 12 个月逐月预扣表。
- **功能要点**：输入每月工资、三险一金、专项附加扣除、其他扣除 → 输出逐月表（累计收入/减除/应纳税所得额/预扣率/速算扣除数/累计应缴/本月预扣）；附"年中税率跳升"说明。
- **存储键**：无（纯计算工具，不落库）。
- **关键规则**：累计减除费用固定 5000×月数；累计应纳税所得额 = 累计收入 − 累计减除 − 累计专项扣除 − 专项附加 − 其他；本月预扣 =（累计应纳税所得额 × 预扣率 − 速算扣除数）− 累计已预扣。预扣率表 `TABLE`（3%~45% 七档，速算扣除数 0/2520/16920/25200/31920/52920/85920）。
- **对外联动**：纯函数 `FW.iitWithholdCalc.withholding` / `bracket` 由 `test/iitwithhold_test.js` 覆盖。

### 7. 财税 / 公式知识 taxkb / fnkb（knowledge.js）

- **定位**：内置参考知识库（财税常识 + 公式），支持分类与搜索。
- **功能要点**：分类导航（税种常识/发票管理/申报期限/身份优惠/账务基础 · Excel函数/财务分析/税务计算/资金时间价值/快捷键）+ 关键词搜索。
- **存储键**：内容为内置常量 `TAX_KB` / `FN_KB`；全局键 `kb_notes` 未使用（预留）。
- **关键规则**：纯展示，无计算。
- **对外联动**：无。

### 8. 报表中心 reports（reports.js）

- **定位**：基于「登记内账」流水自动汇总的老板视角报表（实务收减支口径，非严格会计准则）。
- **功能要点**：利润表（收入−成本−税金−费用−固购=净利润）；资金状况表（简化资产负债表，自动平衡校验）；现金流量表（经营/投资/筹资）；统计期间切换（本月/季/年/全部）、打印、导出 CSV。
- **存储键**：读 `internal`；账户余额复用 `FW.internalCalc.accountBalances` / `getOpeningsTotal`。
- **关键规则**：
  - 支出分类 `classify`：`cost`(材料/采购)、`tax`(税费/印花)、`invest`(设备/固购/装修)、其余 `fee`。
  - 收入还原：实际收入 = 到账金额 + 已扣支出（`deduct`）；已扣支出计入成本只计一次。
  - 退款 `refund` 冲减对应分类支出，不计入总收入。
  - 净利润 = 收入 −(成本+税金+费用+固购)；注明**所得税依实际申报填列，不自动计提**。
- **对外联动**：依赖 `FW.internalCalc`；与 `tax.js` 报表口径不同、互不复用。导出 `FW.reportsCalc`。

### 9. 发票台账 invoices（invoices.js）

- **定位**：进项/销项发票、合同台账、库存台账（增值税测算含留抵）。
- **功能要点**：进项发票（采购，含抵扣状态 未勾选/已勾选/已认证/不抵扣）；销项发票（销售）；自动汇总（可抵扣/待勾选/销项税额/增值税测算含留抵）；合同台账（上传 PDF/Word/Excel 自动提取字段）；库存台账（出入库/退货/结存）。
- **存储键**：`invoices`、`contracts`、`stock`（均按账本隔离）。
- **关键规则**：
  - 增值税测算：`可抵扣 = 进项中"已勾选"+"已认证"税额`；`vat = 销项税额 − 可抵扣`；**vat<0 即留抵**，`carryForward = −vat`；"未勾选"单列待勾选，"不抵扣"不计入。
  - 价税合计 = 不含税金额 + 税额。
  - 合同/库存汇总分别按状态、出入方向归集，结存 `balance = inQ − outQ`。
- **对外联动**：导出 `FW.invoiceCalc / contractCalc / stockCalc`；与 `tax` 模块增值税测算**无直接联动**（发票台账独立测算）。

### 10. 工资登记 salary（salary.js）

- **定位**：逐月登记员工底薪/奖金/提成（可按项目/客户分类），自动累计汇总。
- **功能要点**：员工管理；月度工资登记（底薪/奖金/提成，可按项目分行明细）；年度/个人累计汇总表、项目工资图；导入解析、导出 Excel/CSV。
- **存储键**：`salary_employees`(员工)、`salary_records`(工资记录，含 `baseItems/bonusItems/commissionItems`)。
- **关键规则**：
  - **不计算个税**：仅做金额登记与汇总（`累计 = 底薪+奖金+提成`），无代扣个税/实发字段。
  - 工资可按项目拆分，供 `project_cost` 归集"工资成本"。
- **对外联动**：被 `project_cost` 读取 `salary_records`，按项目汇总工资成本。

### 11. 项目核算 projectCost（project_cost.js）

- **定位**：按项目汇总收入/支出/工资成本，核算利润、利润率与投入产出比（单产）。
- **功能要点**：项目盈亏总表（收入/流水成本/应收回款项/工资成本/总成本/利润/利润率/成本率/投入产出比）；利润瀑布图、逐月趋势、利润率横向对比；成本结构下钻、未分配提醒、排名+下钻；签收单量 `project_qty` 与单产；净额收入校正（补填已扣支出）；导出 Excel。
- **存储键**：`project_qty`(产量)；读 `internal`(流水)、`salary_records`(工资)、`contacts`(预付)。
- **关键规则**：
  - `总成本 = 流水成本 − 应收回款项 + 工资成本`。
  - `利润 = 收入 − 总成本`；`利润率 = 利润/收入×100%`；`投入产出比 = 收入/总成本`。
  - 收入还原：实际收入 = 到账净额 + 已扣支出（`deduct`）。
  - 应收回款项：来自 `contacts` 中"预付"且带关联项目、未用完余额，从总成本中扣除。
- **对外联动**：读取 internal/salary_records/contacts，是内账下游聚合层；被 `dashboard` 读取；导出 `FW.projectCostCalc`。

### 12. 版本与备份 version（version.js）

- **定位**：版本日志 + 数据备份恢复（含照片凭证）。
- **功能要点**：版本日志（版本号=commit 短哈希、日期、改动、状态，当前高亮，可请求回滚）；数据备份（导出整库 JSON，含全部账本与照片）；数据恢复（导入 JSON）；备份/恢复日志。
- **存储键**：`fw_rollback_request`、`fw_backup_log`（直接写 localStorage 全局）；实质数据走 `FW.db.exportAll/importAll`（覆盖全部账本键 + IndexedDB 照片）。
- **关键规则**：导出含 `_app:'财务工作台'`、`_version:3`、`raw`(文本键)、`photos`(照片)；导入支持 merge 去重（按 id + 内容指纹）；回滚请求仅记录，实际恢复由开发者 git 重新部署，**账本数据不受影响**。
- **对外联动**：通过 `FW.db` 统一读写全部模块数据；导入后刷新账本切换器。

---

## 四、跨模块数据流与联动表

| 数据源模块        | 导出接口                                    | 消费模块                             | 共享数据 / 说明                 |
| ------------ | --------------------------------------- | -------------------------------- | ------------------------- |
| internal     | `FW.internalCalc.*`                     | dashboard, reports, project_cost | 流水 / 期初 / 预算              |
| internal     | （被写入）                                   | contacts（核销写）                    | 应收收款记收入 / 应付付款记支出         |
| tax          | —                                       | —                                | 独立"外账"体系，与 internal 不互通   |
| contacts     | —                                       | project_cost（读）                  | 预付→项目核算"应收回款项"            |
| project_cost | `FW.projectCostCalc.*`                  | dashboard                        | 项目盈亏                      |
| reports      | `FW.reportsCalc.*`                      | —                                | 读 internal + internalCalc |
| invoices     | `FW.invoiceCalc/contractCalc/stockCalc` | （未见其它模块调用）                       | 独立增值税测算                   |
| salary       | `FW.salaryCalc` 等                       | project_cost（读）                  | 工资成本按项目归集                 |
| version      | `FW.db.*`                               | 全部模块                             | 经 `FW.db` 备份/恢复全量数据       |

---

## 五、两套独立账套（核心认知）

| 账套   | 模块                                | 记账方式 | 报表口径         | 互通性              |
| ---- | --------------------------------- | ---- | ------------ | ---------------- |
| 登记内账 | internal / reports / project_cost | 单式流水 | 实务收减支（老板视角）  | 三者共享 internal 数据 |
| 报税记账 | tax                               | 复式凭证 | 标准财报（凭证自动汇总） | 独立，与内账不共用        |

> 理解本系统的关键：**内账**用于日常资金与经营分析，**报税记账**用于对外账务与申报，两套数据平行存在、互不取数。

---

## 六、已知缺口与预留项

| 项目         | 现状  | 说明                                               |
| ---------- | --- | ------------------------------------------------ |
| 个税计算       | 已实现 | 「知识工具 → 🧾 个税累计预扣表」(iitwithhold.js) 按累计预扣法出全年逐月表；salary 仍仅登记金额不自动算个税 |
| 申报日历 / 申报表 | 预留  | `tax_filings`、`tax_calendar_custom` 已在隔离键声明，但未实现 |
| 知识库笔记      | 预留  | `kb_notes` 全局键未使用，知识为内置常量                        |
| 发票→税务联动    | 缺失  | 发票台账增值税测算与 tax 模块无直接联动                           |
| 所得税计提      | 不自动 | 报表中心注明"所得税依实际申报填列，不自动计提"                         |
